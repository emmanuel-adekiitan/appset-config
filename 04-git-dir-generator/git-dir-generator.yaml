apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: app1-git-directories
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error

  generators:
    - git:
        repoURL: https://github.com/emmanuel-adekiitan/appset-config.git
        revision: main
        directories:
          - path: "02-app1-config-repo/*"

  template:
    metadata:
      name: "app1-git-{{.path.basename}}"
    spec:
      project: default

      source:
        repoURL: https://github.com/emmanuel-adekiitan/appset-config.git
        targetRevision: main
        path: "{{.path.path}}/manifests"

      destination:
        name: "{{.path.basename}}"
        namespace: "app1-{{.path.basename}}-ns"

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true